2026.03.21
     
     实现管道通信控制mplayer播放,启动write_fifo创建管道cmd_fifo
     在终端输入命令，写入cmd_fifo
     mplayer读取cmd_fifo，并执行命令
     用于此后音频播放控制

     搭建智能音箱程序框架
     1.初始化监听集合
     2.初始化音乐播放链表，双向链表，方便实现播放控制
     3.初始化共享内存
     4.创建makefile脚本
          ipcs 命令显示进程间通信信息，包括共享内存、消息队列、信号量
          ipcrm 命令用于删除进程间通信对象-m是删除共享内存，-q是删除消息队列，-s是删除信号量
          编写一个.sh脚本，用于删除之前运行的进程间通信对象，防止占用
     5.网络初始化
          将套接字绑定到指定端口
          套接字描述符添加到监听集合中
          实现一个本地服务端，用于测试
          json 格式上报数据，这里服务端调试出现了一个无法循环接收数据的问题
               1. memset(buf, 0, sizeof(buf)) 刷新缓冲区
               清除残留数据：清空缓冲区中上次接收的数据残留
               防止数据污染：避免新旧数据混合导致解析错误
               确保数据完整性：为下次接收准备干净的缓冲区
               2. size = 0  刷新计数器
               重置接收计数器：将接收计数器归零，准备接收下一个数据包
               状态同步：确保接收状态与实际数据包边界对齐
               循环复用：使接收逻辑能够在每次循环中正确工作
               3. 问题根源分析
               缓冲区复用：同一个缓冲区用于接收多个数据包
               状态累积：未重置状态变量会导致接收逻辑错误
               数据边界混淆：不清空缓冲区可能导致数据包边界识别错误
               4.可能出现的具体错误
               参数错误：recv(clientfd, buf+20, -16, 0) 导致函数调用失败
               数据错位：新数据写入缓冲区错误位置
               长度解析错误：解析出错误的后续数据长度
               无限循环：在头部接收循环中陷入死循环

2026.03.22
     1.ALSA音频框架
          ALSA 是 Advanced Linux Sound Architecture（高级 Linux 声音架构）的缩写。
          它是现代 Linux 系统中用于处理音频和 MIDI（音乐设备数字接口）的核心软件框架和驱动程序集合。
          主要包括：
          驱动driver：支持各种类型的音频设备，包括板载声卡、USB 音频设备、HDMI 音频输出以及专业的音频接口。
          用户库lib：为应用程序提供统一的 API 接口，使开发者无需直接操作内核驱动即可编写音频软件。
          工具集utils：常用工具有alsamixer：图形化的音量控制终端界面。
                              aplay / arecord：用于播放和录制音频文件。
                              amixer：命令行下的混音器控制工具。
                              aconnect：用于管理 MIDI 连接。
     2.配置混音器
          初步设想将打开、连接、注册、加载、设置等环节打包封装，但是实际上封装后无法及时获取最新音量数据
     3.主进程中创建一个线程，每5秒按照json格式上传数据到服务器

     4.实现向服务器申请数据

2026.03.23
     1.创建父子孙三个进程
     2.孙进程实现简单音乐播放功能
     3.通过共享内存，孙进程写入当前播放音乐名称和播放模式

2026.03.24
     1.实现按照链表顺序播放功能
     2.孙进程通过发送信号给父进程、子进程说明播放完成
     3.收到信号后，子进程结束，父进程将申请新的音乐链表并启动新的子进程
     4.新的子进程将fork()新的孙进程

2026.03.25
     1.引用信号量同步父子孙三进程对共享内存的写入操作
     2.完成各种播放操作：开始、暂停、终止、继续、上一曲、下一曲、音量调节、播放模式改变等

2026.04.11
     1.网络播放功能实现：开始、暂停、终止、继续、上一曲、下一曲、音量调节、播放模式改变等
     2.解决音量设置由于整数除法反复消除小数部分，小于预设值的bug，解决方案：将音量百分比+5之后整除10再乘10

2026.04.15
     1.为全志H618开发板烧录系统
     2.通过枚举类型实现状态机，完成长按、短按、双击几种不同按键输入（单独在开发板上验证过按键，但是集成到项目中还未验证）

2026.04.16
     1.安装交叉编译工具链、配置环境变量、
     2.验证交叉编译工具链（计划明天）
     3.为开发板交叉编译ALSA\JSON等库（未完成，计划明天）
     4.scp 是 Secure Copy 命令，用来通过 SSH 在两台机器之间安全传文件。
          常见用法：
               本地传到远程      scp 本地文件 user@远程IP:远程路径
               远程拉到本地      scp user@远程IP:远程文件 本地路径
               传目录（递归）    scp -r 本地目录 user@远程IP:远程路径
               指定端口（比如 2222）   scp -P 2222 文件 user@ip:/path
               结合你现在的场景（把交叉编译产物传到板子）：scp server_test root@192.168.50.50:/home/

2026.04.17 上午
     1.完成交叉编译链路打通：修正 makefile 工具链路径与变量错误，恢复 ARM 固定编译版本并确保可正常编译。
     2.完成第三方库适配：拉取并交叉编译 alsa-lib、json-c，并根据实际安装结果修正 json-c 头文件与库路径为 third_party/json-c/usr/include、third_party/json-c/usr/lib。
     3.完成测试程序联调：在 test 目录下明确 server_test.c 的交叉编译参数（-I/-L/-ljson-c -lpthread），解决 json-c/json.h 找不到问题。
          服务端交叉编译命令：aarch64-none-linux-gnu-gcc server_test.c -o server_test \
                              -I../third_party/json-c/usr/include \
                              -L../third_party/json-c/usr/lib \
                              -ljson-c -lpthread
     4.完成开发板启动问题定位与修复：处理 init.sh not found（改为按可执行文件目录查找脚本）及 shmat: Permission denied（补充 IPC 权限并兼容已存在资源）。
     5.完成板端音量控制故障定位：通过 amixer 确认控件名为 lineout volume，定位到 DEVICE_ELEMENT 大小写不匹配导致 Error finding mixer element。
     6.完成本地回环验证，下一步计划将服务端部署到主机，联调开发板与主机网络通信链路。

2026.04.17 晚上
     1.排查 sherpa-onnx 语音识别启动报错，定位为命令换行断裂与设备参数传递问题，并整理为可直接执行的规范命令格式。 
     2.完成离线 TTS 播放链路调试，确认 --device-name 参数用法，结合 aplay -l 结果将播放设备从无效 0,0 调整为可用声卡方案。 
     3.排查关键词检测未触发问题，确认模型与录音链路已正常启动，并通过调整 --keywords-score、--keywords-threshold 给出更易触发的测试参数。 
     4.整理并维护测试命令文档 test/language_model_test_command.txt，按统一多行续行格式补充语音合成、语音识别和关键词检测命令。 
     5.新增项目级 Skill .cursor/skills/daily-summary-github-publish/SKILL.md，用于每日总结后执行 GitHub 提交与推送的标准流程。
     6.命令工具：
          查看播放设备     aplay -l 
          查看录音设备     arecord -l
     7.完善发布流程规范：补充 .gitignore 排除策略、HTTPS/PAT 认证操作、TLS 网络重试方法，并更新发布 Skill 规则与安全约束（token 泄露即撤销重建）。


2026.04.18 

     1.完成 asr/makefile 多轮整理与修正：从交叉编译模板出发补齐 ALSA/Sherpa 头库路径、目标文件与链接参数，逐步定位并处理 -lsherpa-onnx-c-api 找不到、链接器选择不当等问题。 
     2.定位并修复 init_asr 隐式声明告警：分析为头文件保护宏与声明可见性问题，调整 alsa.h 宏名与声明位置，消除编译阶段函数声明异常。 
     3.完成 Sherpa 动态库加载问题排查：通过 ldconfig 与库路径配置解决 libsherpa-onnx-c-api.so 运行时找不到，打通 ASR 可执行文件启动链路。 
     4.定位并解决 FIFO 启动阻塞问题：确认 open("/home/fifo/asr_fifo", O_WRONLY) 在无读端时阻塞，后续改为 O_RDWR 规避卡住；同时排查并修复 FIFO 文件属主/权限导致的 open fifo error。 
     5.完成板端声卡设备适配：根据实际设备将录音设备参数调整为可用节点（hw:0,0），解决 snd_pcm_open error，使主流程恢复可运行。 
     6.完成关键词识别失败根因定位：确认代码唤醒词与 keywords.txt 实际词条不一致（如代码用“你好小胖”，模型词表无此项），统一关键词文本。 
     7.命令工具： 安装 ALSA 开发包 apt install -y libasound2-dev 板端安装 json-c apt install -y libjson-c-dev，并统一开发板“系统默认路径”编译思路。 

2026.04.19
    1.排查 ASR 启动报「open fifo error」：根因为 init.sh 未创建 asr_fifo，已补充 mkfifo，并在 main.c 用 perror 提示缺失管道。
    2.排查语音「暂停播放」无效：定位为 select.c 中 strstr( buf, "播放" ) 抢先匹配「暂停播放」，属判断顺序/子串过宽问题（若已本地调整可一并保留）。
    3.梳理播放上一首/下一首异常：手动 loadfile 与 child_process 曲终 link_find_next 叠加导致短暂回跳、连跳两首；链表用 strstr 定位当前曲易误判。
    4.完成播放与链表修复：
          link 侧按路径最后一段文件名精确匹配；
          共享内存增加 prev_leave_basename、manual_seek_at、post_seek_retry_used；
          曲终逻辑支持手动切歌后短时重试同一首、上一首后曲终跳过「刚离开那首」；
          init_shm attach 时清零新字段防旧段脏数据。
    5.联调结论：切歌与上报行为已按预期工作，相关问题已闭环。

2026.04.20
     1.完成语音合成功能的实现，关键词唤醒后会有语音应答。
     2.完成唤醒词的替换，你好小七。
     3.tts语音合成进程和music_player主进程会同时打开播放设备，冲突导致无法播放，
          需要引入混音插件asound.conf
          将tts中设备设置为default


2026.04.21
     1.player.c中播放命令指定硬件的部分导致asound.conf无作用，删除硬件指定命令，音箱正常工作。
     下一步三个工作：
     1.云端服务器部署
     2.QT软件开发
     3.阿里或者deepseek大模型接入。